<Window x:Class="KargoRaf.Views.WidgetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kargolar"
        Width="260" Height="340"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        FontFamily="{StaticResource AppFont}">

    <Border CornerRadius="20"
            BorderBrush="#66FFFFFF"
            BorderThickness="1"
            Background="#CCFFFFFF"
            Opacity="0.96"
            MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="4" Opacity="0.22" Color="#0F172A"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" CornerRadius="20,20,0,0" Padding="12,10"
                    Background="#CC2563EB">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Image Source="pack://application:,,,/Assets/AppIcon.png"
                               Width="22" Height="22" Margin="0,0,8,0"
                               Stretch="Uniform"
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="Kargolar" Foreground="White" FontWeight="Bold" FontSize="14"/>
                    </StackPanel>
                    <Button Content="✕"
                            HorizontalAlignment="Right"
                            Width="24" Height="24"
                            Padding="0"
                            Background="#33FFFFFF"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="CloseButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="12">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,10,0,6">
                <TextBlock Text="{Binding TotalCount}"
                           FontSize="36" FontWeight="Bold"
                           Foreground="{StaticResource PrimaryBrush}"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="aktif" FontSize="10" FontWeight="SemiBold"
                           Foreground="{StaticResource MutedTextBrush}"
                           HorizontalAlignment="Center"/>
            </StackPanel>

            <ItemsControl Grid.Row="2" ItemsSource="{Binding SectionCounts}" Margin="10,0,10,6">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#66EFF6FF"
                                CornerRadius="8" Margin="2" Padding="8,4" MinWidth="36">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="{Binding Label}" FontSize="9" FontWeight="Bold"
                                           Foreground="{StaticResource PrimaryBrush}"
                                           HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Count}" FontSize="15" FontWeight="Bold"
                                           Foreground="{StaticResource TextBrush}"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Border Grid.Row="3" Margin="10,0,10,10"
                    Background="#55F1F5F9"
                    CornerRadius="14"
                    ClipToBounds="True"
                    Padding="8,6">
                <ScrollViewer x:Name="TickerScroll"
                              VerticalScrollBarVisibility="Hidden"
                              HorizontalScrollBarVisibility="Disabled"
                              IsHitTestVisible="False">
                    <ItemsControl ItemsSource="{Binding TickerItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="6,5" Margin="0,1"
                                        Background="#88FFFFFF"
                                        CornerRadius="8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Background="{StaticResource PrimaryBrush}"
                                                CornerRadius="6" Padding="6,2" Margin="0,0,8,0">
                                            <TextBlock Text="{Binding SectionOrder}"
                                                       Foreground="White" FontSize="10" FontWeight="Bold"/>
                                        </Border>
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding Name}"
                                                   FontSize="12" FontWeight="SemiBold"
                                                   Foreground="{StaticResource TextBrush}"
                                                   VerticalAlignment="Center"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>
        </Grid>
    </Border>
</Window>
