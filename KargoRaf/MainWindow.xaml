<Window x:Class="KargoRaf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:KargoRaf.ViewModels"
        xmlns:controls="clr-namespace:KargoRaf.Controls"
        Title="Kargo Raf"
        Width="1360" Height="900"
        MinWidth="1024" MinHeight="720"
        Background="{StaticResource BgBrush}"
        FontFamily="{StaticResource AppFont}"
        WindowStartupLocation="CenterScreen"
        PreviewKeyDown="MainWindow_PreviewKeyDown">

    <Window.InputBindings>
        <KeyBinding Key="W" Modifiers="Control" Command="{Binding ToggleWidgetCommand}"/>
        <KeyBinding Key="Delete" Command="{Binding DeliverSelectedCommand}"/>
    </Window.InputBindings>

    <Grid Margin="24,20,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <controls:TopBarControl x:Name="TopBar" Grid.Row="0" Margin="0,0,0,16"/>

        <controls:QuickAddPanel x:Name="QuickAdd" Grid.Row="1" Margin="0,0,0,14"/>

        <controls:SummaryStripControl Grid.Row="2"/>

        <ItemsControl Grid.Row="3"
                      VerticalAlignment="Stretch"
                      ItemsSource="{Binding Sections}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="{Binding SectionGridColumns}"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:SectionCardViewModel}">
                    <controls:SectionCardControl/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <controls:SnackbarControl Grid.Row="4" Margin="0,12,0,0"/>
    </Grid>
</Window>
